/// Public barrel for the injection module. Mirrors
/// `lalafen/lib/injection/injection.dart` — a single re-export so
/// call sites only need `package:servicar/injection/injection.dart`.
library;

export 'global_providers.dart';
